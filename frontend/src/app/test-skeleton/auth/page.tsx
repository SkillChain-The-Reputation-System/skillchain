import AuthSkeleton from "@/components/skeleton/auth-skeleton";


export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <AuthSkeleton></AuthSkeleton>
    );
  }
  